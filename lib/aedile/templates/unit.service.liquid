# content for unit {{name}}
[Unit]
Description={{description}}
After=docker.service
Requires=docker.service

[Service]
ExecStart=/usr/bin/docker run {{image}} {{command}}

[X-Fleet]
X-Conflicts={{conflict_glob}}